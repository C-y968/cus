services:
  compass:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:4380:4380"
    volumes:
      - compass-data:/app/data
    environment:
      TRUST_PROXY: "1"
      COOKIE_SECURE: "1"
      PUBLIC_URL: "https://customers.example.com"
volumes:
  compass-data:
