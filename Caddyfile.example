# Replace with a domain whose DNS already points to your server.
# Run Caddy on the same host as the loopback-only published application port.
customers.example.com {
  request_body {
    max_size 210MB
  }
  reverse_proxy 127.0.0.1:4380
}
